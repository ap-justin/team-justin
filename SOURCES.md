# Official Sources Registry

**Team principle — official sources first.** For any framework / library / API / CLI / cloud-service specifics, no agent answers from training data. Resolve it through the official MCP, skill, or plugin below. Prefer first-party sources over third-party blogs. Training data is a starting hypothesis to verify, never the answer.

Agents: consult the row for the stack you're working in. The PM (the `lead` skill) uses this table to route and to wire any newly-minted specialist to its official source.

| Stack / domain | Official source (use this) | How |
|---|---|---|
| **Any library/framework/API** (universal fallback) | **Context7 MCP** | `resolve-library-id` → `query-docs`. Their global rule already mandates this. |
| **Svelte / SvelteKit** | **Svelte MCP** + `svelte:*` skills | `mcp__svelte__list-sections` → `get-documentation`; validate with `svelte-autofixer`; `svelte:svelte-core-bestpractices`. Optional `svelte:svelte-file-editor` agent. |
| **React Router** (framework / data / declarative / RSC) | **`react-router` skill** (vendored, official) → installed docs → Context7 | Invoke the `react-router` skill (mode-aware); it defers to `node_modules/react-router/docs/` as truth. Context7 (`react-router` v7) is the fallback. Prefer a repo's own `.agents/skills/react-router` if present. For the React components rendered inside routes (hooks/effects/keys/memo/a11y/TSX craft — RR is the only framework, so the seat carries React skills too) also run **`vercel:react-best-practices`** as a quality pass: ~80% of its rules are pure React/JS/DOM (apply verbatim), but it's Vercel's so translate the Next/RSC minority (`server-*` category, `next/dynamic`) to RR7 loaders/actions/`React.lazy` rather than blind-copying. Complements — never overrides — the `react-router` skill on RR APIs. |
| **Postgres + drivers** (Drizzle / Prisma / postgres.js / pg / Kysely) → `postgres-architect` | **Context7**; for **Drizzle** also its official **`llms.txt`** | Resolve the exact driver id before writing queries/migrations. Drizzle: `https://orm.drizzle.team/llms.txt` (per-dialect schema/migrations/query + drizzle-kit + 40+ provider connections) as first-party index, Context7 (`/drizzle-team/drizzle-orm-docs`) for exact API. Do **not** add third-party community skill packs for a driver already covered here (violates official-sources-first). |
| **Auth** (Better Auth) → `better-auth-specialist` | **Better Auth docs MCP** (`https://mcp.better-auth.com/mcp`) → official **Skills** → **`llms.txt`** → **Context7** (`/better-auth/better-auth`) | Prefer the docs MCP if `mcp__better-auth__*` tools are connected; install via `npx auth@latest mcp --claude-code` (or `mcp.json`: `{ "better-auth": { "url": "https://mcp.better-auth.com/mcp" } }`). Official agent skills: `npx skills add better-auth/skills`. Fallbacks: `https://better-auth.com/llms.txt` (index; `llms-full.txt` full) and Context7. **Not currently connected in this session → use Context7 + llms.txt.** Note: this docs MCP ≠ the Better Auth `mcp()` *library plugin* (which makes an app an OAuth/MCP provider). |
| **Headless UI components** (Ark UI: React/Vue/Solid/Svelte) → the **`ark-ui` skill** (repo-authored, `skills/ark-ui/`; loaded by the UI framework builders — Ark is ambient, not a seat, as of v0.34.0) | **Ark UI MCP** (`npx -y @ark-ui/mcp`) → **`llms.txt`** → **Context7** (`ark-ui`) | Load the `ark-ui` skill for the playbook (reach-for-Ark judgment, part anatomy, a11y, styling hookup). Install the MCP: `claude mcp add ark-ui -- npx -y @ark-ui/mcp`. Tools: `list_components`, `list_examples`, `get_example` (framework-aware), `styling_guide` (design tokens / `data-*` attrs / CSS vars). Fallback: `https://ark-ui.com/docs/ai/llms.txt`, then Context7. Ark is headless/unstyled on Zag.js state machines — apply `design-director`'s tokens via the styling guide; compose the full part anatomy so the built-in a11y fires; don't hand-roll ARIA/focus. Brownfield: defer to the repo's existing component lib (shadcn/Radix/…) — Ark for greenfield/Vue-Solid-Svelte/already-on-Ark. This single-purpose official docs MCP is fine (same shape as the Better Auth / Svelte MCPs) — no fleet. |
| **TypeScript** (language + type system + compiler config) → the **`typescript` skill** (repo-authored, `skills/typescript/`; loaded by every TS-writing builder — TS is ambient, not a seat) | **Context7** (`typescript` — pin the installed major) + the official **tsconfig reference** (`typescriptlang.org/tsconfig`) + the compiler's self-docs; curated baseline: Matt Pocock's **tsconfig cheat sheet** (`totaltypescript.com/tsconfig-cheat-sheet`) | Load the `typescript` skill for the playbook. Confirm every tsconfig option and type-system behavior against docs, never memory. Seed a *fresh* tsconfig from the cheat-sheet baseline (base: `target es2022`, `moduleDetection force`, `isolatedModules`, `verbatimModuleSyntax`, `esModuleInterop`, `skipLibCheck`; strict: `strict` + `noUncheckedIndexedAccess` + `noImplicitOverride`; bundler → `module preserve`+`noEmit`, tsc-emit → `module nodenext`), then verify each option against the reference. Read the *effective* config/cost: `tsc --showConfig` (extends chains hide options), `tsc --explainFiles`, `--extendedDiagnostics`/`--generateTrace` for slow checks. **Extend the framework's generated tsconfig** (SvelteKit `.svelte-kit/tsconfig.json`, Next's `next` plugin, `astro/tsconfigs/*`, `@tsconfig/*`) — don't fork it. Covers the compiler config, the type layer (generics/conditional/mapped/branded types, discriminated unions, inference, `.d.ts`, `declare module`), ESM/CJS, monorepo project references (`tsc -b`), JS→TS migration, and the typecheck gate (`tsc --noEmit`/`svelte-check`); **excludes** the **formatter/linter and the monorepo task/package graph** (Biome/ESLint/Prettier + pnpm + Turborepo — that's the **`toolchain-engineer`** seat's, next row). Repo-wide TS-infra work = the lead dispatches a general agent with this skill. |
| **Repo tooling / build orchestration** (pnpm workspaces + Turborepo task graph/caching + Biome/ESLint/Prettier lint/format) → `toolchain-engineer` | **pnpm docs** (`pnpm.io`) + **vendored `turborepo` skill** (`skills/turborepo/`) + **Biome docs** (`biomejs.dev`, `llms.txt`) + **Context7** (`pnpm`, `turbo`, `biome`; `eslint`/`prettier` for brownfield) | Owns the tooling substrate, not app code: `pnpm-workspace.yaml`/catalogs/lockfile → `turbo.json` (tasks, `dependsOn`, `outputs`, cache keys, env passthrough, boundaries) → `biome.json`. Layering: pnpm (package graph) → Turbo (task graph) → Biome (quality gate). Seam with `typescript`: that skill owns `tsconfig` *content* (incl. `tsc -b` project references); this seat owns the `typecheck`/`lint`/`build`/`test` **tasks** that run it + their cache inputs. pnpm is the team greenfield default (the `lead` skill) — this seat owns that default's config thereafter; **brownfield: match the repo's actual PM + linter, never rip-and-replace**. Biome's own `.claude/skills` are contributor-facing (developing Biome) — not used here. |
| **Next.js** (App Router) → `nextjs-builder` | **`vercel:*` skills** + Vercel MCP | `vercel:nextjs`, `vercel:next-cache-components`, `vercel:shadcn`, `vercel:react-best-practices`; Context7 (`next.js`) fallback. |
| **Web performance** (CWV, caching, rendering, bundle) → `vercel-perf-optimizer` | **`vercel:*` skills** + Vercel MCP | `vercel:performance-optimizer`, `vercel:next-cache-components`, `vercel:runtime-cache`; MCP `get_runtime_logs`/`get_deployment_build_logs` for real data. |
| **Vercel platform-ops** (deploy/CI-CD, env/secrets, Functions/edge runtime, Cron, domains, Firewall/WAF, AI Gateway, Marketplace/storage provisioning, Routing Middleware) → `vercel-platform-engineer` | **`vercel:*` skills** + Vercel MCP | `vercel:deployments-cicd`, `vercel:vercel-cli`, `vercel:env-vars`, `vercel:vercel-functions`, `vercel:vercel-firewall`, `vercel:routing-middleware`, `vercel:ai-gateway`, `vercel:workflow`, `vercel:marketplace`, `vercel:vercel-storage`; MCP for real project/deploy state. **Three Vercel-touching seats, three lanes**: app code = `nextjs-builder`; web perf/CWV/caching-for-speed = `vercel-perf-optimizer`; deploy/infra/security = this seat. The caching overlap: perf owns cache *decisions for speed*, this seat owns the deploy/runtime-cache *plumbing*. |
| **Sanity CMS** → `sanity-builder` | **`sanity:*` skills** + Sanity MCP | `sanity:sanity-best-practices`, `sanity:content-modeling-best-practices`; `get_schema` before querying. |
| **Cloudflare** (Workers, Pages, D1, KV, R2, Durable Objects, Queues, Workflows, Vectorize, Hyperdrive) → `cloudflare-builder` | **Cloudflare MCP** + **vendored `cloudflare` + `wrangler` skills** (`skills/cloudflare/`, `skills/wrangler/`) + Context7 | MCP (`mcp__claude_ai_Cloudflare_Developer_Platform__*` + `search_cloudflare_documentation`) for live account state + docs; the vendored `cloudflare` skill (SKILL.md indexes `references/` per product, loaded on demand) + `wrangler` skill for CLI/config; Context7 (`cloudflare-workers`, `wrangler`) fallback. **D1 (CF's SQLite) is this seat's, not `postgres-architect`'s**; Postgres-from-a-Worker = Hyperdrive in front of `postgres-architect`'s DB. |
| **Integrations** (Linear, Notion, Drive, PayPal, Stripe) | their **official MCP** | Use the connected MCP server, not scraped docs. |
| **Image/asset generation** → `graphic-designer` | **`scripts/gen-asset.ts`** (`@google/genai`) + Context7 (`/googleapis/js-genai`) | Run the bundled gen script (`npm --prefix "${CLAUDE_PLUGIN_ROOT}" run gen-asset -- …`); needs `GOOGLE_API_KEY`. Verify model ids/params via Context7, never memory. |
| **Ambient hero video** → `graphic-designer` | **`scripts/gen-asset.ts --video`** (Veo via `@google/genai`) + `ffmpeg` | Async Veo generation → webm + mp4 + poster still; needs `GOOGLE_API_KEY` and `ffmpeg`/`ffprobe` on PATH. Verify Veo model ids/params via Context7 (`/googleapis/js-genai`), never memory. |
| **True-alpha cutout / background removal** → `graphic-designer` | **`scripts/gen-asset.ts --cutout`** (rembg, local) | `--cutout --input <img>` → real 4-channel-alpha webp/png; runs fully local (no API key), self-bootstraps a python venv on first use. `u2net_human_seg` for people, `--rembg-model u2net` for objects. Do NOT use a gemini `--input` edit for cutouts — it recomposites to an opaque matte. |
| **Code-generated generative art** (p5.js flow fields / particles / parametric) → `graphic-designer` | **vendored `algorithmic-art` skill** (official Anthropic skill; `skills/algorithmic-art/`) + Context7 (`p5.js`) | Invoke `/algorithmic-art` for the playbook — it emits a philosophy `.md` + generative `.js` + interactive `.html` viewer (code, not baked media). The **branch by aesthetic**, not a video substitute: photoreal/organic atmosphere → Veo video (row above); geometric/interactive/lightweight/seeded/brand-parametric → algorithmic-art. Bind the philosophy to `design-director`'s solid stepped palette; seed for reproducibility. Verify p5.js API via Context7, never memory. |
| **Technical SEO + AEO** (metadata, OG, sitemaps/robots, canonical/hreflang, JSON-LD, AI-answer readiness) → `seo-engineer` | **`sanity:seo-aeo-best-practices` skill** (general, despite namespace) + the stack's meta API; optional **SearchFit SEO** plugin for automated audit/schema-gen | Invoke the skill for the checklist; verify the framework mechanism via its row (Next.js → `vercel:nextjs`/Vercel MCP for the Metadata API + `sitemap.ts`/`robots.ts`; others → Context7 for `<svelte:head>`/Astro head). Validate JSON-LD against schema.org. If installed, SearchFit's `/seo-check` + `/generate-schema` are tools the seat vets and applies — not a parallel agent stack to hand off to. No Anthropic-native SEO skill exists (checked). CWV is `vercel-perf-optimizer`'s. |
| **Design color system** (stepped scales, semantic step roles, dark mode) → `design-director` (enforced by `taste-reviewer`) | **Radix Colors — "Understanding the scale"** (`https://www.radix-ui.com/colors/docs/palette-composition/understanding-the-scale`) | The *principle, not the palette*: express every color as an ordered set of **solid** steps with fixed jobs (1–2 bg · 3–5 component bg+states · 6–8 borders · 9–10 solid · 11–12 text) — bespoke brand scales, not Radix's stock hues. **Hard rule: never opacity/alpha as a lightener** (translucency is for glass/scrim/disabled only); every shade the UI needs is its own solid step, dark mode a second solid scale. `taste-reviewer` FAILs opacity-as-lightener. |
| **Module/interface design + structural integrity** → `architecture-reviewer` | **`codebase-design` skill** + Context7 | Invoke `codebase-design` for the deep-module vocabulary (seams, interface depth, coupling); Context7 for framework-specific seam rules (what belongs in a loader vs component, etc.). |
| **Testing** → `test-writer` | **the project's own testing skill/docs first**, then `/tdd` skill + Context7 (per runner) | Local sources first: read `.claude/skills/*test*` / `TESTING.md` / existing tests + config, and follow them verbatim. `/tdd` for red-green discipline; Context7 for the exact runner's API (`vitest`, `jest`, `playwright`, `@testing-library/*`) — never assert test-API shapes from memory. If the repo has no written testing knowledge, `test-writer` captures it (see its Step 3). |
| **Rendered-UI visual review** → `visual-reviewer` | **`local-browser` skill** (`agent-browser`) | Drive the live app via `local-browser` (persistent session; dev server must already be running — never start it). Screenshot per viewport for the vision pass; `agent-browser eval` for measured evidence (`getBoundingClientRect` / `getComputedStyle`). Hold the render against `design-director`'s plan + project design tokens. Visual-regression diffing (Percy/Playwright snapshots) is a separate CI concern. |
| **Accessibility (a11y) audit** (WCAG 2.1 AA: contrast, keyboard, focus, touch targets, SR, ARIA; post-build) → `visual-reviewer` or lead inline | **vendored `accessibility-review` skill** (from `anthropics/knowledge-work-plugins` → `design`) | Invoke `/accessibility-review` after a page/component renders — the a11y sibling of `visual-reviewer` (pixels) and `taste-reviewer` (slop). When a dev server is up, gather **measured** evidence via `local-browser` (`getComputedStyle` contrast, `getBoundingClientRect` target size, focus order) rather than eyeballing. File findings via `TRACKER.md` (user-level plan-store files) when tickets are wanted. Vendored (not the live `design` plugin) so its 9 MCP servers stay out. |
| **UX design** (user research, user flows + IA, mockup-stage usability critique, UX copy, design→eng handoff spec; **upstream** of visual design) → `ux-designer` | **vendored `design`-plugin skills** (from `anthropics/knowledge-work-plugins` → `design`): `user-research`, `research-synthesis`, `design-critique`, `ux-copy`, `design-handoff` | Invoke the matching skill as the playbook: `/user-research` (plan/interview guide), `/research-synthesis` (transcripts→themes — the *usability* lens; PM's `/synthesize-research` is the *product* lens), `/design-critique` (mockup-stage, upstream of `taste-reviewer`/`visual-reviewer`), `/ux-copy` (microcopy), `/design-handoff` (eng spec sheet). Sits **upstream of `design-director`** (which owns the visual system + `design-system` — not vendored here). No Figma MCP in this team → work from brief/screenshots/repo/`design-director`'s plan, not a live pull. Vendored (not the live `design` plugin) so its 9 MCP servers stay out. |
| **Conversion copy + CRO on shipped pages** (marketing/landing-page copy, copy-editing, page/form conversion) → `conversion-copywriter` | **vendored marketing skills** (from `coreyhaines31/marketingskills`, copied into `skills/`): `copywriting`, `copy-editing`, `cro` | Invoke the matching skill as the playbook: `/copywriting` (write/rewrite page copy), `/copy-editing` (seven-sweeps line edit + expert-panel scoring), `/cro` (impact-ordered page/form conversion audit). **No first-party source exists for copywriting** — Context7's fallback is for libraries/APIs, not persuasion craft — so a curated community pack from a recognized SaaS-marketing authority (Corey Haines) is the legitimate backing, same tier as the mattpocock pack; this does **not** violate official-sources-first (there is no official source to prefer). Content sibling of `seo-engineer` (that row owns the meta *markup*; this seat writes the meta *copy*). Distinct from `ux-copy` (in-product microcopy). Vendored (not a live plugin), cross-skill mentions to unvendored channel skills degrade gracefully — see Vendored resources. |
| **Motion/animation craft** (what should animate + how it feels: easing, duration, origin, interruptibility, frequency, gestures/springs) → `motion-engineer` | **vendored Emil Kowalski skills** (from `emilkowalski/skills`, copied into `skills/`): `find-animation-opportunities`, `emil-design-eng`, `apple-design`, `animation-vocabulary`, `review-animations`, `improve-animations` | Load the skill for the mode: **advise** — `/find-animation-opportunities` (the restraint gate — frequency/purpose/speed/function), `/emil-design-eng` (decision framework + Before/After format), `/apple-design` (gesture/drag/spring/momentum, interruptibility), `/animation-vocabulary` (name a loosely-described effect); **review** — `/review-animations` (10 non-negotiable standards + escalation triggers + `STANDARDS.md` precise curves/durations), `/improve-animations` (codebase-wide audit → self-contained fix plans via `AUDIT.md`). **No first-party animation source exists** — Context7 covers libraries, not motion craft — so this authored pack from the reference source (Emil Kowalski: Sonner, Vaul, animations.dev) is the legitimate backing, same posture as the marketing pack; does **not** violate official-sources-first. Motion sibling of `taste-reviewer`/`visual-reviewer`; craft peer of `design-director` (that row owns motion *signature*, this seat owns the *precise recipe + review*). Extend the repo's existing `--ease-*`/`--duration-*` tokens, never invent parallel ones. |
| **Product / roadmap** (prioritized roadmap, per-item briefs, research/competitive/metrics synthesis, stakeholder updates) → `product-manager` | **vendored Product-Management skills** (from `anthropics/knowledge-work-plugins`, copied into `skills/`) + `TRACKER.md` | Invoke the vendored plain-name skills as the playbook: `/roadmap-update` (Now/Next/Later, quarterly themes, OKR), `/synthesize-research`, `/competitive-brief`, `/metrics-review`, `/stakeholder-update`, `/product-brainstorming` — vet output, and persist to the user-level plan store (`~/.claude/team-justin/management/<project-slug>/roadmap/` — `ROADMAP.md` + per-item briefs under `briefs/`, horizon + `ready-for-planning` as frontmatter). **Not vendored: `write-spec`** (PRD is `planner`/to-spec) **and `sprint-planning`** (execution sequencing is `planner`/to-tickets + lead). Vendored (not the live plugin) so the 16 auth-gated MCP servers it bundled stay out — see Vendored resources. Sits **upstream of `planner`**: roadmap+brief → planner spec+tickets. |
| **Planning / decomposition** (spec, tracer-bullet tickets, wayfinder map for work bigger than one context) → `planner` | **vendored `to-spec` / `to-tickets` / `wayfinder` skills** + `TRACKER.md` | The planner reads the three vendored skills as its playbook (minus their HITL steps — the lead owns those) and persists the plan as **user-level files** under `~/.claude/team-justin/management/<project-slug>/plan/<effort>/` (`spec.md`/`map.md` + one file per ticket under `tickets/`) via `TRACKER.md` — the skills' own local-markdown path, made canonical (per-ticket files with `id`/`status`/`blocked_by` frontmatter, edges by id, frontier by query, sequential one-effort-at-a-time). `TRACKER.md` **replaces** `/setup-matt-pocock-skills` for this team — never run that. |
| **Authoring skills + agents** (this repo's own maintenance) | **`writing-great-skills` skill** (installed via the mattpocock pack) | Invoke `/writing-great-skills` (+ its `GLOSSARY.md`) before writing/editing any `agents/*.md` or `skills/*` — invocation model (model- vs user-invoked), information hierarchy, progressive disclosure, single-source-of-truth, aggressive pruning. The standard for "Growing the team" (see `ROSTER.md`). Reused, not vendored — already in `~/.claude/skills/`. |
| **Astro** *(planned specialist)* | **Context7** (`astro`) + astro docs | Until a dedicated `astro-builder` exists, use the general path + Context7. |

**When a stack has no row:** use Context7 as the fallback, and tell the PM so it can add a row here (and, if the stack recurs, recommend minting a dedicated specialist — see `ROSTER.md`).

## Vendored resources
Official upstream skills copied into this repo (kept verbatim; refresh periodically):
- `skills/react-router/` ← `remix-run/react-router` `main:.agents/skills/react-router` (MIT). Refresh: re-download `SKILL.md` + `references/*.md` from `raw.githubusercontent.com/remix-run/react-router/main/.agents/skills/react-router/`. Symlinked at `~/.claude/skills/react-router`.
- `skills/algorithmic-art/` ← `anthropics/skills` `main:skills/algorithmic-art` (official Anthropic skill; `LICENSE.txt` kept verbatim). The `graphic-designer` seat's code-generated-art path (p5.js generative), alongside its gen-asset script paths. Vendored because it's not installed via a plugin (same reason as `react-router`). Refresh: re-download `SKILL.md` + `LICENSE.txt` + `templates/*` from `raw.githubusercontent.com/anthropics/skills/main/skills/algorithmic-art/`. Symlinked at `~/.claude/skills/algorithmic-art`.
- `skills/{to-spec,to-tickets,wayfinder}/` ← `mattpocock/skills` `main:skills/engineering/*` (planner's backing playbook). Kept verbatim; the HITL steps are run by the lead, the tracker substrate is supplied by `TRACKER.md` (not `/setup-matt-pocock-skills`). Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/engineering/{to-spec,to-tickets,wayfinder}/SKILL.md`. Symlinked at `~/.claude/skills/{to-spec,to-tickets,wayfinder}`.
- `skills/accessibility-review/` ← `anthropics/knowledge-work-plugins` → `design` plugin (v1.2.0, sha 15898ec). A team-adapted copy (connector hooks remapped to `local-browser`/`TRACKER.md`). Vendored so the `design` plugin's 9 MCP servers stay out. Symlinked at `~/.claude/skills/accessibility-review`.
- `skills/{user-research,research-synthesis,design-critique,ux-copy,design-handoff}/` ← `anthropics/knowledge-work-plugins` → `design` plugin (v1.2.0, sha 15898ec). The `ux-designer` seat's playbook (`design-system` now vendored too — see the reuse/maintenance entry below). Vendored so the `design` plugin's 9 MCP servers stay out. Each carries a provenance header; symlinked into `~/.claude/skills/`.
- `skills/{roadmap-update,synthesize-research,competitive-brief,metrics-review,stakeholder-update,product-brainstorming}/` ← `anthropics/knowledge-work-plugins` → `product-management` plugin (v1.2.0, sha 15898ec). The `product-manager` seat's playbook. `write-spec`/`sprint-planning` deliberately not vendored (planner's lane). Vendored so the plugin's 16 MCP servers stay out. Each carries a provenance header; symlinked into `~/.claude/skills/`.
- `skills/{copywriting,copy-editing,cro}/` ← `coreyhaines31/marketingskills` (`main`, sha 286d371; SKILL.md + `references/*`, verbatim). The `conversion-copywriter` seat's playbook. A **community pack**, vendored (unlike the first-party sources above) because no official copywriting source exists — Corey Haines is a recognized SaaS-marketing authority, so this is a vetted known-good pack, same tier as the mattpocock engineering skills. Only these three of the repo's ~47 skills are taken (the on-page, ships-in-the-build ones); `image`/`video` are already `graphic-designer`'s, the SEO cluster is `seo-engineer`'s, and channel/campaign skills stay out of scope. Cross-skill mentions to unvendored skills (`emails`/`popups`/`offers`/`ab-testing`/`marketing-psychology`) degrade gracefully. Each carries a provenance header; symlinked into `~/.claude/skills/`. Refresh: re-download from `raw.githubusercontent.com/coreyhaines31/marketingskills/main/skills/{copywriting,copy-editing,cro}/`.
- `skills/{find-animation-opportunities,emil-design-eng,apple-design,animation-vocabulary,review-animations,improve-animations}/` ← `emilkowalski/skills` (`main`, sha 6bf2443; SKILL.md + aux `AUDIT.md`/`PLAN-TEMPLATE.md`/`STANDARDS.md`, verbatim; MIT `LICENSE` per dir). The `motion-engineer` seat's playbook. An **authored pack** from the reference web-animation source (Emil Kowalski — Sonner, Vaul, animations.dev), vendored (like the marketing pack, unlike the first-party sources above) because no official animation source exists — a vetted known-good pack, same posture. All six of the repo's skills are taken (all are motion craft). `review-animations` keeps `disable-model-invocation: true` (the seat invokes it explicitly). Each `SKILL.md` carries a provenance header; symlinked into `~/.claude/skills/`. Refresh: re-download from `raw.githubusercontent.com/emilkowalski/skills/main/skills/{find-animation-opportunities,emil-design-eng,apple-design,animation-vocabulary,review-animations,improve-animations}/`.
- `skills/turborepo/` ← `vercel/turborepo` `main:skills/turborepo` (MIT; `LICENSE` kept verbatim). The `toolchain-engineer` seat's monorepo task-graph/caching playbook (SKILL.md + `command/` + `references/`). Vendored (not plugin-installed) like `react-router`; the pnpm + Biome halves use official docs, not vendored. Refresh: re-download from `raw.githubusercontent.com/vercel/turborepo/main/skills/turborepo/`. Symlinked at `~/.claude/skills/turborepo`.
- `skills/cloudflare/` + `skills/wrangler/` ← `cloudflare/skills` `main` (Apache-2.0; `LICENSE` kept verbatim). The `cloudflare-builder` seat's platform + CLI playbook — `cloudflare` is the comprehensive skill (SKILL.md indexes ~60 product `references/*`, loaded on demand), `wrangler` the CLI skill. Vendored the two user-facing core skills; the specialized siblings (`durable-objects`, `agents-sdk`, `sandbox-sdk`, `workers-best-practices`, `cloudflare-one`) stay in the upstream repo, installable via the CF plugin/`npx skills` if a project needs them. Works alongside the connected Cloudflare MCP. Refresh: re-download `skills/{cloudflare,wrangler}/` from the `cloudflare/skills` repo. Symlinked at `~/.claude/skills/{cloudflare,wrangler}`.

Reuse/maintenance skills — now **vendored** (was "reused from `~/.claude/skills`"); vendored so the plugin is self-contained on Claude Code web, where the host `~/.claude` isn't visible:
- `skills/{codebase-design,tdd,diagnosing-bugs,domain-modeling}/` ← `mattpocock/skills` `main:skills/engineering/*` (sha 9603c1c). `codebase-design` (deep-module vocabulary, backs `architecture-reviewer`), `tdd` (backs `test-writer` + the lead's test-first loop), `diagnosing-bugs` (backs `vercel-perf-optimizer` + the lead's debug loop), `domain-modeling` (shared). Kept verbatim; no per-dir LICENSE (mattpocock ships only a root LICENSE — same precedent as `to-spec`/`to-tickets`/`wayfinder`). Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/engineering/{codebase-design,tdd,diagnosing-bugs,domain-modeling}/`.
- `skills/{grilling,writing-great-skills}/` ← `mattpocock/skills` `main:skills/productivity/*` (sha 9603c1c). `grilling` (plan/design stress-test, backs `planner` + `product-manager` + the lead's grilling step), `writing-great-skills` (the authoring standard for growing the team). Kept verbatim, no per-dir LICENSE. Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/productivity/{grilling,writing-great-skills}/`.
- `skills/design-system/` ← `anthropics/knowledge-work-plugins` → `design` plugin (v1.2.0, sha 15898ec; subpath `design/skills/design-system`). The `ux-designer` seat's design-system audit/document/extend playbook — previously left out as `design-director`'s lane, now vendored so the seat's full playbook works on web without the plugin's MCP fleet. Refresh from the `design` plugin.
- `skills/design-taste-frontend/` ← `leonxlnx/taste-skill` (tasteskill) (`main`, sha 7c397f2; subpath `skills/taste-skill`; MIT `LICENSE` kept verbatim). The anti-slop frontend taste playbook backing `design-director` + `taste-reviewer`. A standalone authored pack, vendored like the marketing/animation packs. Refresh from `raw.githubusercontent.com/leonxlnx/taste-skill/main/skills/taste-skill/`.
- `skills/local-browser/` ← **locally authored** (this repo), a thin wrapper over the `agent-browser` CLI (vercel-labs/agent-browser) that backs `visual-reviewer`'s rendered-UI checks. No upstream skill exists → no re-sync source; maintain it here.

The install is now plugin-based (see README) — the "Symlinked at `~/.claude/skills`" notes in the entries above are legacy local-install context, not a requirement.

Svelte needs no vendoring — its official AI surface (MCP, `svelte:*` skills, `svelte:svelte-file-editor` subagent) is already installed; llms fallback at `svelte.dev/llms.txt`.
